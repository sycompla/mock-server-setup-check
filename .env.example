# Backend Configuration for WMS Application
# Copy this file to .env and adjust values for your environment

# Backend Mode
# Values: real | mock
# - real: Connect to actual SAP Business One backend (requires backend server running)
# - mock: Use local JSON mock data for offline development
BACKEND_MODE=real

# Mock Data Directory
# Path relative to webapp directory where mock JSON files are located
# Only used when BACKEND_MODE=mock
MOCK_DATA_PATH=/mock

# Real Backend URLs (only used when BACKEND_MODE=real)
# These are the default values, uncomment and modify if needed

# SAP B1 Service Layer
# REAL_BACKEND_B1S=/b1s/v2

# ODBC Service
# REAL_BACKEND_ODBC=./ODBC

# WMS Backend Services (License, Printer)
# REAL_BACKEND_WMS=/wms

# Installation/Login Service
# REAL_BACKEND_INSTALL=/install

# Push Notification API
# REAL_BACKEND_API=./api

# Notes:
# - After changing BACKEND_MODE, restart the development server
# - This file should NOT be committed to git (it's in .gitignore)
# - Each developer can have their own .env with custom settings
